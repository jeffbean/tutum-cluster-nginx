weba:
  image: tutum/hello-world
  environment:
    - VIRTUAL_HOST=webapp.local
webb:
  image: tutum/hello-world
  environment:
    - VIRTUAL_HOST=webapp.local
webc:
  image: tutum/hello-world
  environment:
    - VIRTUAL_HOST=webapp.local
webaa:
  image: tutum/hello-world
  environment:
    - VIRTUAL_HOST=webapp2.local

nginx:
  build: .
  links:
    - weba
    - webaa
    - "webb:webb"
    - "webc:webc"
  ports:
   - "80:80"